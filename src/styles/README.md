# Styles

Global CSS variables live in `src/app/globals.css` and are mirrored in
`tailwind.config.ts` as semantic design tokens. Keep component styling on tokens
such as `background`, `foreground`, `surface`, `primary`, `accent`, `danger` and
`border` instead of raw palette values.
